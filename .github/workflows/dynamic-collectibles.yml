# DEPRECATED — replaced by floors.yml (daily). The 3-hourly floor/listings refresh
# is no longer used: the collectible floor now comes once a day from scraper.floors
# (VeVe floorMarketPrice, tracker fallback). Schedule removed so this never runs.
# Safe to delete this file on GitHub.
name: "[deprecated] VeVe dynamic data sync (collectibles)"
on: { workflow_dispatch: {} }
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated: collectible floors now run daily via 'VeVe floors sync' (floors.yml)."
